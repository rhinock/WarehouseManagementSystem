using WMS.DTO;
using WMS.Models;

namespace WMS.Mappers
{
    //public class ItemsMapper : BaseMapper<ItemDto, Item>
    //{
    //    protected override void InitMap()
    //    {
    //        Map(x => x.Id, x => x.Id);
    //        Map(x => x.Name, x => x.Name);
    //        Map(x => x.Price, x => x.Price);
    //    }
    //}
}
